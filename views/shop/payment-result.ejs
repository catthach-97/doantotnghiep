<%- include('../includes/head.ejs') %>
    <title><%= pageTitle %> - Shoe Store</title>
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="0">
    <style>
        /* Custom styles for payment result page */
        .payment-container {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            min-height: 100vh;
        }
        
        .payment-card {
            background: white;
            border-radius: 1rem;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }
        
        .success-icon {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            border-radius: 50%;
            box-shadow: 0 8px 25px rgba(16, 185, 129, 0.3);
        }
        
        .error-icon {
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
            color: white;
            border-radius: 50%;
            box-shadow: 0 8px 25px rgba(239, 68, 68, 0.3);
        }
        
        .btn-primary-payment {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 0.75rem;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }
        
        .btn-primary-payment:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
        }
        
        .btn-secondary-payment {
            background: white;
            color: #64748b;
            border: 2px solid #e2e8f0;
            border-radius: 0.75rem;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        
        .btn-secondary-payment:hover {
            background: #f8fafc;
            border-color: #cbd5e1;
        }
        
        .support-info {
            background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
            border-radius: 1rem;
            border: 1px solid #e2e8f0;
        }
    </style>
</head>

<body class="payment-container">
    <!-- Navigation -->
    <%- include('../includes/navigation.ejs') %>

    <!-- Payment Result Section -->
    <main class="container mx-auto px-4 py-16">
        <div class="max-w-2xl mx-auto">
            <div class="payment-card p-8 text-center">
                <% if (success) { %>
                    <!-- Success Icon -->
                    <div class="w-20 h-20 success-icon flex items-center justify-center mx-auto mb-6">
                        <i class="ri-check-line text-4xl"></i>
                    </div>

                    <h1 class="text-3xl font-bold text-gray-800 mb-4">Thanh toán thành công!</h1>
                    <p class="text-gray-600 mb-6">
                        <%= message %>
                    </p>

                            <div class="bg-gray-50 rounded-lg p-6 mb-6">
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-left">
                                    <div>
                                        <span class="text-sm text-gray-500">Mã đơn hàng:</span>
                                        <p class="font-semibold text-gray-800">#<%= orderId %>
                                        </p>
                                    </div>
                                    <% if (typeof transactionNo !=='undefined' ) { %>
                                        <div>
                                            <span class="text-sm text-gray-500">Mã giao dịch:</span>
                                            <p class="font-semibold text-gray-800">
                                                <%= transactionNo %>
                                            </p>
                                        </div>
                                        <% } %>
                                            <% if (typeof formattedAmount !=='undefined' ) { %>
                                                <div>
                                                    <span class="text-sm text-gray-500">Số tiền:</span>
                                                    <p class="font-semibold text-green-600">
                                                        <%= formattedAmount %>đ
                                                    </p>
                                                </div>
                                                <% } %>
                                                    <div>
                                                        <span class="text-sm text-gray-500">Thời gian:</span>
                                                        <p class="font-semibold text-gray-800">
                                                            <%= new Date().toLocaleString('vi-VN') %>
                                                        </p>
                                                    </div>
                                </div>
                            </div>

                    <div class="flex flex-col sm:flex-row gap-4 justify-center">
                        <a href="/orders"
                            class="btn-primary-payment px-6 py-3 rounded-lg font-semibold transition-all duration-300 hover:scale-105">
                            <i class="ri-file-list-line mr-2"></i>
                            Xem đơn hàng
                        </a>
                        <a href="/"
                            class="btn-secondary-payment px-6 py-3 rounded-lg font-semibold transition-all duration-300">
                            <i class="ri-home-line mr-2"></i>
                            Về trang chủ
                        </a>
                    </div>
                <% } else { %>
                    <!-- Error Icon -->
                    <div class="w-20 h-20 error-icon flex items-center justify-center mx-auto mb-6">
                        <i class="ri-close-line text-4xl"></i>
                    </div>

                    <h1 class="text-3xl font-bold text-gray-800 mb-4">Thanh toán thất bại!</h1>
                    <p class="text-gray-600 mb-6">
                        <%= message %>
                    </p>

                                <div class="bg-gray-50 rounded-lg p-6 mb-6">
                                    <div class="text-left">
                                        <div class="mb-2">
                                            <span class="text-sm text-gray-500">Mã đơn hàng:</span>
                                            <p class="font-semibold text-gray-800">#<%= orderId %>
                                            </p>
                                        </div>
                                        <div>
                                            <span class="text-sm text-gray-500">Thời gian:</span>
                                            <p class="font-semibold text-gray-800">
                                                <%= new Date().toLocaleString('vi-VN') %>
                                            </p>
                                        </div>
                                    </div>
                                </div>

                    <div class="flex flex-col sm:flex-row gap-4 justify-center">
                        <a href="/cart"
                            class="btn-primary-payment px-6 py-3 rounded-lg font-semibold transition-all duration-300 hover:scale-105">
                            <i class="ri-shopping-cart-line mr-2"></i>
                            Thử lại thanh toán
                        </a>
                        <a href="/"
                            class="btn-secondary-payment px-6 py-3 rounded-lg font-semibold transition-all duration-300">
                            <i class="ri-home-line mr-2"></i>
                            Về trang chủ
                        </a>
                    </div>
                <% } %>
                    </div>

            <!-- Support Info -->
            <div class="mt-8 text-center">
                <div class="support-info p-6">
                    <p class="text-gray-600 mb-4 font-medium">Cần hỗ trợ? Liên hệ với chúng tôi:</p>
                    <div class="flex justify-center space-x-8">
                        <a href="tel:1900123456" class="text-blue-600 hover:text-blue-800 transition-colors flex items-center">
                            <i class="ri-phone-line mr-2 text-xl"></i>
                            <span class="font-semibold">1900 123 456</span>
                        </a>
                        <a href="mailto:support@shoestore.com"
                            class="text-blue-600 hover:text-blue-800 transition-colors flex items-center">
                            <i class="ri-mail-line mr-2 text-xl"></i>
                            <span class="font-semibold">support@shoestore.com</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <%- include('../includes/footer.ejs') %>

    <!-- Custom JavaScript -->
    <script src="/js/main.js"></script>
    <script>
        // Xóa giỏ hàng khỏi localStorage sau khi thanh toán thành công
        <% if (success) { %>
            localStorage.removeItem('cart');
            console.log('🧹 Cart cleared from localStorage after successful payment');
            
            // Cập nhật cart count trong navigation
            updateCartCount(0);
        <% } %>
        
    </script>
</body>

</html>