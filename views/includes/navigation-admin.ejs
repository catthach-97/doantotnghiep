<%- include('head.ejs') %>
<nav class="bg-blue-900 text-white shadow-sm sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <!-- Logo -->
      <div class="flex items-center gap-2">
        <a href="/admin/dashboard" class="text-2xl font-pacifico font-bold text-white flex items-center gap-2">
          <i class="ri-store-2-line text-3xl"></i> <span>Shoe Store</span>
        </a>
      </div>
      <!-- Admin Navigation -->
      <div class="hidden md:flex items-center space-x-2">
        <a href="/admin/dashboard" class="px-4 py-2 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/dashboard' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-dashboard-line"></i> Dashboard
        </a>
        <a href="/admin/products" class="px-4 py-2 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/products' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-shopping-bag-line"></i> Quản lý sản phẩm
        </a>
        <a href="/admin/inventory" class="px-4 py-2 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/inventory' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-store-2-line"></i> Quản lý kho
        </a>
        <a href="/admin/orders" class="px-4 py-2 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/orders' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-shopping-cart-line"></i> Đơn hàng
        </a>
        <a href="/admin/accounts" class="px-4 py-2 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/accounts' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-user-settings-line"></i> Tài khoản
        </a>
        <a href="/admin/reviews" class="px-4 py-2 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/reviews' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-star-line"></i> Quản lý đánh giá
        </a>
      </div>
      <!-- User dropdown -->
      <div class="relative ml-4">
        <button type="button" class="flex items-center space-x-2 text-sm font-medium text-blue-200 hover:text-white focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 focus:ring-offset-blue-900" onclick="toggleUserMenu()">
          <div class="h-8 w-8 rounded-full bg-blue-500 flex items-center justify-center">
            <i class="ri-user-line text-white text-sm"></i>
          </div>
          <span class="hidden lg:block">Admin User</span>
          <i class="ri-arrow-down-s-line h-4 w-4"></i>
        </button>
        
        <!-- Dropdown menu -->
        <div id="userMenu" class="absolute right-0 z-10 mt-2 w-48 origin-top-right rounded-md bg-white py-1 shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none hidden">
          <a href="/admin/profile" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">
            <i class="ri-user-line mr-3 text-gray-400"></i>
            Profile
          </a>
          <a href="/admin/settings" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">
            <i class="ri-settings-3-line mr-3 text-gray-400"></i>
            Settings
          </a>
          <a href="/logout" class="flex items-center px-4 py-2 text-sm text-red-600 hover:bg-gray-100">
            <i class="ri-logout-box-line mr-3 text-red-500"></i>
            Đăng xuất
          </a>
        </div>
      </div>
      <!-- Mobile menu button -->
      <button class="md:hidden p-2" id="admin-mobile-menu-btn">
        <i class="ri-menu-line text-2xl"></i>
      </button>
    </div>
    <!-- Mobile Navigation -->
    <div class="md:hidden hidden" id="admin-mobile-menu">
      <div class="py-4 space-y-2">
        <a href="/admin/dashboard" class="block px-4 py-3 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/dashboard' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-dashboard-line"></i> Dashboard
        </a>
        <a href="/admin/products" class="block px-4 py-3 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/products' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-shopping-bag-line"></i> Quản lý sản phẩm
        </a>
        <a href="/admin/inventory" class="block px-4 py-3 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/inventory' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-store-2-line"></i> Quản lý kho
        </a>
        <a href="/admin/orders" class="block px-4 py-3 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/orders' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-shopping-cart-line"></i> Đơn hàng
        </a>
        <a href="/admin/accounts" class="block px-4 py-3 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/accounts' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-user-settings-line"></i> Tài khoản
        </a>
        <a href="/admin/reviews" class="block px-4 py-3 rounded-lg transition-all duration-200 flex items-center gap-2 <%= path === '/admin/reviews' ? 'bg-gradient-to-r from-blue-600 to-blue-700 text-white shadow-lg' : 'text-blue-200 hover:text-white hover:bg-blue-800/50' %>">
          <i class="ri-star-line"></i> Quản lý đánh giá
        </a>
        <form action="/logout" method="POST" class="block px-4 py-3">
          <button type="submit" class="w-full text-left text-red-400 hover:text-red-600 flex items-center gap-2"><i class="ri-logout-box-line"></i> Đăng xuất</button>
        </form>
      </div>
    </div>
  </div>
</nav>

<script>
function toggleUserMenu() {
    const userMenu = document.getElementById('userMenu');
    userMenu.classList.toggle('hidden');
}

// Close user menu when clicking outside
document.addEventListener('click', function(event) {
    const userMenu = document.getElementById('userMenu');
    const userMenuButton = event.target.closest('[onclick="toggleUserMenu()"]');
    
    if (!userMenuButton && !userMenu.contains(event.target)) {
        userMenu.classList.add('hidden');
    }
});
</script>
<script>
document.addEventListener('DOMContentLoaded', function() {
  const btn = document.getElementById('admin-mobile-menu-btn');
  const menu = document.getElementById('admin-mobile-menu');
  if(btn && menu) {
    btn.addEventListener('click', function() {
      menu.classList.toggle('hidden');
    });
  }
});
</script> 